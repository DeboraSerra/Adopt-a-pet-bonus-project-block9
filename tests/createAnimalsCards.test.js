/**
 * @jest-environment jsdom
 */


const createAnimalsCards = require('../script2');

describe('Test the function createAnimalCards', () => {
  it('verifies if ', () => {
    expect().toBe();
  });
  it('verifies if ', () => {
    expect().toBe();
  })
})